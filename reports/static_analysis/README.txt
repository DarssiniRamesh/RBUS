Static Analysis Reports for RBUS
================================

Generated by: Automated CI Agent
Date: [populate as needed]

Contents:
- clang-tidy: Summary.md (narrative summary of issues)
- cppcheck: cppcheck_report.txt (full findings from cppcheck)

How these were produced:
1) Configured build to generate compile_commands.json:
   cmake -S . -B build/rbus -DCMAKE_EXPORT_COMPILE_COMMANDS=ON -DBUILD_FOR_DESKTOP=ON -DENABLE_UNIT_TESTING=ON -DCMAKE_BUILD_TYPE=Debug

2) clang-tidy run (using compile database):
   run-clang-tidy -p build/rbus -header-filter='^(?!.*build).*' -checks='-*,readability-*,bugprone-*,performance-*,portability-*,clang-analyzer-*' 'src/*' 'include/*'

3) cppcheck run:
   cppcheck --enable=all --inconclusive --std=c11 --quiet --force --inline-suppr --suppress=missingIncludeSystem -i build -i build/rbus -i .git -i docs -i utils .

Notes:
- build/ and generated directories were excluded where possible.
- clang-tidy output was extremely verbose; the Summary.md captures key issues and patterns.

Recommendations:
- Use these reports to create targeted refactoring tasks, starting with high-severity categories in Summary.md.
